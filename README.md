# Information-Retrieval-Tokenizer-Program
Information Retrieval Tokenizer Program in Python that reads a text file and tokens the text file. The program prints the tokens or the unqiue words and the number of the unique words.  Part B takes two text files from the command line as arguments and outputs the number of tokens they have in common.
